# Live SMTP credentials (Mandrill API key). The tracked file is .env.example.
.env

# Real recipient list -- personal addresses stay off GitHub.
# The tracked file is List_Of_People_To_Send_To.example.csv.
List_Of_People_To_Send_To.csv

# Jupyter writes copies of every file it opens here, including the recipient CSV.
.ipynb_checkpoints/

# Dry runs are scratch output, never an issue.
.dryrun-*/

# Tectonic's build log -- machine noise, regenerable from the committed report.tex.
*/report.log

# Run logs are the verbatim transcript of an agent that reads .env and the recipient
# CSV during the same run, so they can quote either. They stay on the machine.
logs/

__pycache__/
*.pyc
